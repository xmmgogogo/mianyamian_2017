------------------------PHP开发APP接口------------------

请求 -> 返回固定格式的数据结构（json，xml）

> json_encode 只支持utf-8格式

缓存技术
* 静态缓存
	1，生成缓存
	2，读取
	3，删除
	
* memcache + redis缓存
	1，...
	
* 定时任务
	1，crontab -l
	2，crontab -e 编辑定时任务
	3，crontab -r 删除定时任务
	
------------------------------------------------------------


------------------------Sphinx------------------------------
> 配合mysql，PHP ，完成存库数据预先缓存，PHP读取。

* 全文搜索	